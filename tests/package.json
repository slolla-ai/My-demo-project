{
  "name": "smart-geofence-alert-report-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for FS-1692 Smart Geofence Alert Health Report UI — px-web-app",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test regression --grep @smoke --headed",
    "test:regression": "playwright test regression",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.4.0"
  }
}
